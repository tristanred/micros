#ifndef ERRORCODES_H
#define ERRORCODES_H

/**
 * System error code list. Trying to follow the error names from Linux as 
 * much as possible. The error codes are not the same but if a name is found in
 * Linux it should have a simmilar meaning.
 */

#define ENOMEM 1 // Out of memory
#define EINVAL 2 // Invalid function argument

#endif
